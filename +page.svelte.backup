<script lang="ts">
	import { onMount } from 'svelte';

	let featureItems = [
		{
			title: 'Live Canvas',
			description: 'Share ideas on an infinite canvas, together.',
			screenshot: 'https://placehold.co/390x844/1a1a1a/ffffff?text=Live+Canvas',
			active: true
		},
		{
			title: 'Instant Feedback',
			description: 'Annotate designs and get comments in real-time.',
			screenshot: 'https://placehold.co/390x844/e30050/ffffff?text=Instant+Feedback',
			active: false
		},
		{
			title: 'Cloud Asset Library',
			description: 'All your components and brand assets, synced.',
			screenshot: 'https://placehold.co/390x844/34a853/ffffff?text=Asset+Library',
			active: false
		}
	];

	let currentScreenshot = featureItems[0].screenshot;
	let currentScreenshotAlt = `Screenshot of AuraFlow's ${featureItems[0].title} feature shown on an iPhone 15 Pro mockup.`;

	let galleryImages = [
		'https://placehold.co/600x800/e83e8c/ffffff?text=UI+Design',
		'https://placehold.co/600x400/fd7e14/ffffff?text=Wireframe',
		'https://placehold.co/600x600/20c997/ffffff?text=Prototype',
		'https://placehold.co/600x500/6610f2/ffffff?text=Collaboration',
		'https://placehold.co/600x700/ffc107/111111?text=Asset+Management',
		'https://placehold.co/600x450/34a853/ffffff?text=Mobile+View'
	];

	let currentLayout = 'masonry';
	let isDragging = false;
	let sliderPosition = 50;

	function handleFeatureHover(index: any) {
		featureItems = featureItems.map((item, i) => ({
			...item,
			active: i === index
		}));
		currentScreenshot = featureItems[index].screenshot;
		currentScreenshotAlt = `Screenshot of AuraFlow's ${featureItems[index].title} feature shown on an iPhone 15 Pro mockup.`;
	}

	function handleSliderDrag(event: any) {
		if (!isDragging) return;
		event.preventDefault();

		const slider = event.currentTarget.closest('.comparison-slider');
		const rect = slider.getBoundingClientRect();
		const clientX = event.clientX || (event.touches && event.touches[0].clientX);
		let offsetX = clientX - rect.left;

		if (offsetX < 0) offsetX = 0;
		if (offsetX > rect.width) offsetX = rect.width;

		sliderPosition = (offsetX / rect.width) * 100;
	}

	function startDrag() {
		isDragging = true;
	}

	function stopDrag() {
		isDragging = false;
	}

	function switchLayout(layout: string) {
		currentLayout = layout;
	}

	onMount(() => {
		// Scroll animations observer
		const observer = new IntersectionObserver(
			(entries) => {
				entries.forEach((entry) => {
					if (entry.isIntersecting) {
						entry.target.classList.add('animate-in');
					}
				});
			},
			{
				threshold: 0.1
			}
		);

		document.querySelectorAll('.reveal').forEach((el) => {
			observer.observe(el);
		});

		// Add global event listeners for slider
		const handleMouseMove = (e: any) => handleSliderDrag(e);
		const handleMouseUp = () => stopDrag();
		const handleTouchMove = (e: any) => handleSliderDrag(e);
		const handleTouchEnd = () => stopDrag();

		window.addEventListener('mousemove', handleMouseMove);
		window.addEventListener('mouseup', handleMouseUp);
		window.addEventListener('touchmove', handleTouchMove);
		window.addEventListener('touchend', handleTouchEnd);

		return () => {
			window.removeEventListener('mousemove', handleMouseMove);
			window.removeEventListener('mouseup', handleMouseUp);
			window.removeEventListener('touchmove', handleTouchMove);
			window.removeEventListener('touchend', handleTouchEnd);
		};
	});
</script>

<svelte:head>
	<title>Meet AuraFlow | The Future of Creative Collaboration</title>
	<meta
		name="description"
		content="Powerful. Intuitive. Creative collaboration tools for modern design teams."
	/>
	<style>
		@media print {
			@page {
				size: A4 landscape;
				margin: 0.5in;
			}

			section {
				page-break-inside: avoid;
				page-break-after: always;
				min-height: 8.27in; /* A4 landscape height */
			}

			section:last-child {
				page-break-after: avoid;
			}

			video,
			.animate-pulse {
				display: none !important;
			}

			.print-hidden {
				display: none !important;
			}

			.print-visible {
				display: block !important;
			}

			body {
				background: white !important;
				color: #161617 !important;
			}

			h1,
			h2,
			h3 {
				color: #0071e3 !important;
			}

			p {
				color: #666 !important;
			}
		}

		@media screen {
			.print-visible {
				display: none;
			}
		}
	</style>
</svelte:head>

<!-- Page 1: Hero Section -->
<section
	id="hero"
	class="page-section relative flex min-h-screen items-center justify-center overflow-hidden bg-[#161617] print:min-h-[8.27in] print:bg-white"
>
	<video
		class="print-hidden absolute top-1/2 left-1/2 h-full w-full -translate-x-1/2 -translate-y-1/2 object-cover opacity-10"
		autoplay
		muted
		loop
		playsinline
	>
		<source
			src="https://www.apple.com/105/media/us/macbook-pro-14-and-16/2021/a7a242b6-a81d-468f-8255-320d3d526277/films/product/macbook-pro-14-and-16-product-tpl-us-2021_16x9.mp4"
			type="video/mp4"
		/>
	</video>

	<div class="reveal relative z-10 text-center text-[#f5f5f7] print:text-[#161617]">
		<h1
			class="text-[clamp(3rem,7vw,5.5rem)] font-semibold tracking-tight text-[#0071e3] print:text-[4rem]"
		>
			Meet AuraFlow
		</h1>
		<p class="mt-4 text-[clamp(1.5rem,3vw,2rem)] font-medium print:text-[1.5rem] print:text-[#666]">
			Powerful. Intuitive. Creative.
		</p>
		<p
			class="mx-auto mt-8 max-w-2xl text-[clamp(1rem,2.5vw,1.25rem)] leading-relaxed text-[#a1a1a6] print:text-lg print:text-[#666]"
		>
			The future of creative collaboration is here. AuraFlow transforms how design teams work
			together, bringing seamless collaboration, real-time feedback, and powerful tools into one
			beautiful platform.
		</p>
	</div>

	<!-- MacBook Hero Display -->
	<div
		class="print-hidden perspective-2000 absolute -bottom-[30%] left-1/2 z-0 w-full max-w-[1600px] [transform:translateX(-50%)_rotateX(60deg)] [transform-style:preserve-3d] md:-bottom-[30%]"
	>
		<div class="macbook-pro relative mx-auto aspect-[16/10] w-[120%]">
			<div
				class="lid h-full w-full rounded-[20px] bg-[#2a2a2c] p-[1.5%] shadow-[0_50px_100px_rgba(0,0,0,0.5)]"
			>
				<div
					class="screen relative h-full w-full overflow-hidden rounded-[12px] bg-black bg-cover bg-center"
					style="background-image: url('https://placehold.co/1600x1000/0071e3/FFFFFF?text=AuraFlow+Dashboard');"
				>
					<div
						class="screen-glow absolute inset-0 animate-pulse rounded-[12px] opacity-20 shadow-[inset_0_0_40px_20px_#0071e3]"
					></div>
				</div>
			</div>
		</div>
	</div>

	<!-- Print-only hero image -->
	<div
		class="print-visible absolute bottom-8 left-1/2 h-64 w-96 -translate-x-1/2 items-center justify-center rounded-2xl bg-[#0071e3] text-2xl font-bold text-white shadow-2xl print:flex"
	>
		AuraFlow Dashboard
	</div>
</section>

<!-- Page 2: Features Section -->
<section
	id="features"
	class="page-section min-h-screen bg-[#161617] py-16 text-[#f5f5f7] print:min-h-[8.27in] print:bg-white print:text-[#161617]"
>
	<div
		class="reveal container mx-auto flex max-w-6xl flex-col items-center gap-12 px-4 lg:flex-row lg:gap-16 print:gap-8"
	>
		<div class="flex-1 text-left print:text-center lg:print:text-left">
			<h2
				class="text-[clamp(2.5rem,6vw,4rem)] leading-tight font-semibold tracking-tight text-[#0071e3] print:text-[3rem]"
			>
				Designed for<br />Creative Collaboration
			</h2>
			<p
				class="mt-5 max-w-2xl text-[clamp(1rem,2.5vw,1.25rem)] leading-relaxed text-[#a1a1a6] print:text-lg print:text-[#666]"
			>
				AuraFlow brings your entire creative process into one place. From initial brainstorms to
				final approvals, it's the tool your team has been waiting for.
			</p>

			<div class="mt-10 space-y-4 print:space-y-6">
				{#each featureItems as item, index}
					<div
						class="feature-item cursor-pointer rounded-xl border border-transparent p-6 transition-all duration-300 hover:border-[rgba(255,255,255,0.15)] hover:bg-[rgba(40,40,40,0.5)] print:cursor-default print:border-[#e0e0e0] print:bg-white print:shadow-md"
						class:active={item.active}
						class:bg-[rgba(40,40,40,0.5)]={item.active}
						class:border-[rgba(255,255,255,0.15)]={item.active}
						class:print:border-[#0071e3]={item.active}
						class:print:bg-blue-50={item.active}
						on:mouseenter={() => handleFeatureHover(index)}
						role="button"
						tabindex="0"
						on:keydown={(e) => e.key === 'Enter' && handleFeatureHover(index)}
					>
						<h3
							class="mb-1 text-2xl font-semibold text-[#f5f5f7] print:text-xl print:text-[#161617]"
						>
							{item.title}
						</h3>
						<p class="text-base text-[#a1a1a6] print:text-[#666]">{item.description}</p>
					</div>
				{/each}
			</div>
		</div>

		<div class="flex flex-1 items-center justify-center lg:mt-12 print:mt-0">
			<div
				class="print-hidden iphone-pro relative w-[240px] rounded-[45px] bg-[#2e2e30] p-3 shadow-[-10px_10px_30px_rgba(0,0,0,0.3),inset_0_0_2px_2px_#1c1c1e] transition-transform duration-300 hover:scale-105 md:w-[280px] lg:hover:[transform:scale(1.05)_rotateY(15deg)]"
				style="height: calc(240px * 2.16); min-height: calc(240px * 2.16);"
			>
				<div
					class="dynamic-island absolute top-4 left-1/2 z-20 h-6 w-24 -translate-x-1/2 rounded-full bg-black md:w-24"
				></div>
				<div
					class="screen relative h-full w-full overflow-hidden rounded-[35px] bg-black bg-cover bg-center"
					style="background-image: url('{currentScreenshot}');"
					aria-live="polite"
				>
					<img src={currentScreenshot} alt={currentScreenshotAlt} class="hidden" />
				</div>
			</div>

			<!-- Print-only feature showcase -->
			<div
				class="print-visible h-96 w-80 items-center justify-center rounded-2xl bg-gradient-to-br from-[#0071e3] to-[#0056b3] text-white shadow-2xl print:flex"
			>
				<div class="p-8 text-center">
					<div class="mb-4 text-4xl">📱</div>
					<h3 class="mb-2 text-xl font-bold">{featureItems.find((f) => f.active)?.title}</h3>
					<p class="text-sm opacity-90">{featureItems.find((f) => f.active)?.description}</p>
				</div>
			</div>
		</div>
	</div>
</section>

<!-- Page 3: Why Switch Section -->
<section
	id="why-switch"
	class="page-section min-h-screen bg-[#161617] py-16 text-[#f5f5f7] print:min-h-[8.27in] print:bg-white print:text-[#161617]"
>
	<div class="reveal container mx-auto max-w-6xl px-4 text-center">
		<h2
			class="mx-auto mb-12 max-w-4xl text-[clamp(2.5rem,6vw,4rem)] leading-tight font-semibold tracking-tight text-[#0071e3] print:text-[2.5rem]"
		>
			Unlike scattered files and endless emails, AuraFlow centralizes your entire workflow
		</h2>
		<p
			class="mx-auto mb-12 max-w-2xl text-[clamp(1rem,2.5vw,1.25rem)] leading-relaxed text-[#a1a1a6] print:text-lg print:text-[#666]"
		>
			Go from cluttered to clear. Experience the transformation from chaotic workflows to
			streamlined collaboration.
		</p>

		<!-- Interactive slider for screen -->
		<div
			class="print-hidden comparison-slider relative mx-auto aspect-video w-[90%] max-w-4xl overflow-hidden rounded-2xl shadow-[0_20px_50px_rgba(0,0,0,0.4)]"
		>
			<div
				class="comparison-image absolute top-0 left-0 h-full w-full bg-cover bg-center"
				style="background-image: url('https://placehold.co/1600x900/444444/999999?text=The+Old+Way');"
			></div>
			<div
				class="comparison-image absolute top-0 left-0 z-10 h-full bg-cover bg-center"
				style="background-image: url('https://placehold.co/1600x900/0071e3/FFFFFF?text=The+AuraFlow+Way'); width: {sliderPosition}%;"
			></div>
			<div
				class="comparison-handle absolute top-0 z-30 h-full w-1 cursor-ew-resize bg-white/70"
				style="left: {sliderPosition}%;"
				on:mousedown={startDrag}
				on:touchstart={startDrag}
				role="slider"
				tabindex="0"
				aria-label="Comparison slider"
				aria-valuemin="0"
				aria-valuemax="100"
				aria-valuenow={sliderPosition}
			>
				<div
					class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 rounded-full bg-black/60 px-2 py-2 text-xs text-white"
				>
					◀︎▶︎
				</div>
			</div>
		</div>

		<!-- Print-friendly comparison -->
		<div class="print-visible mx-auto grid max-w-4xl grid-cols-2 gap-8 print:block">
			<div class="text-center">
				<div
					class="mb-4 flex h-64 items-center justify-center rounded-xl bg-gray-400 text-2xl font-bold text-white"
				>
					The Old Way
				</div>
				<div class="space-y-2 text-left">
					<div class="flex items-center text-red-600 print:text-red-800">
						<span class="mr-2">❌</span> Scattered files across platforms
					</div>
					<div class="flex items-center text-red-600 print:text-red-800">
						<span class="mr-2">❌</span> Endless email chains
					</div>
					<div class="flex items-center text-red-600 print:text-red-800">
						<span class="mr-2">❌</span> Version control nightmares
					</div>
					<div class="flex items-center text-red-600 print:text-red-800">
						<span class="mr-2">❌</span> Delayed feedback loops
					</div>
				</div>
			</div>
			<div class="text-center">
				<div
					class="mb-4 flex h-64 items-center justify-center rounded-xl bg-[#0071e3] text-2xl font-bold text-white"
				>
					The AuraFlow Way
				</div>
				<div class="space-y-2 text-left">
					<div class="flex items-center text-green-600 print:text-green-800">
						<span class="mr-2">✅</span> Everything in one place
					</div>
					<div class="flex items-center text-green-600 print:text-green-800">
						<span class="mr-2">✅</span> Real-time collaboration
					</div>
					<div class="flex items-center text-green-600 print:text-green-800">
						<span class="mr-2">✅</span> Automatic version control
					</div>
					<div class="flex items-center text-green-600 print:text-green-800">
						<span class="mr-2">✅</span> Instant feedback & approvals
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<!-- Page 4: Dynamic Gallery Section -->
<section
	id="dynamic-gallery"
	class="page-section min-h-screen bg-[#161617] py-16 text-[#f5f5f7] print:min-h-[8.27in] print:bg-white print:text-[#161617]"
>
	<div class="reveal container mx-auto max-w-7xl px-4 text-center">
		<h2
			class="text-[clamp(2.5rem,6vw,4rem)] leading-tight font-semibold tracking-tight text-[#0071e3] print:text-[2.5rem]"
		>
			A canvas for your biggest ideas
		</h2>
		<p
			class="mx-auto mt-5 max-w-2xl text-[clamp(1rem,2.5vw,1.25rem)] leading-relaxed text-[#a1a1a6] print:text-lg print:text-[#666]"
		>
			From wireframes to high-fidelity prototypes, see what you can create with AuraFlow.
		</p>

		<div class="print-hidden mt-10 mb-10">
			<button
				class="toggle-btn mx-1 rounded-full border px-5 py-2 text-sm transition-all duration-200"
				class:bg-[#0071e3]={currentLayout === 'layered'}
				class:text-white={currentLayout === 'layered'}
				class:border-[#0071e3]={currentLayout === 'layered'}
				class:bg-[rgba(40,40,40,0.5)]={currentLayout !== 'layered'}
				class:text-[#a1a1a6]={currentLayout !== 'layered'}
				class:border-[rgba(255,255,255,0.15)]={currentLayout !== 'layered'}
				on:click={() => switchLayout('layered')}
			>
				Layered View
			</button>
			<button
				class="toggle-btn mx-1 rounded-full border px-5 py-2 text-sm transition-all duration-200"
				class:bg-[#0071e3]={currentLayout === 'masonry'}
				class:text-white={currentLayout === 'masonry'}
				class:border-[#0071e3]={currentLayout === 'masonry'}
				class:bg-[rgba(40,40,40,0.5)]={currentLayout !== 'masonry'}
				class:text-[#a1a1a6]={currentLayout !== 'masonry'}
				class:border-[rgba(255,255,255,0.15)]={currentLayout !== 'masonry'}
				on:click={() => switchLayout('masonry')}
			>
				Grid View
			</button>
		</div>

		<!-- Interactive gallery for screen -->
		<div
			class="print-hidden gallery-container transition-all duration-500"
			class:columns-1={currentLayout === 'masonry'}
			class:md:columns-2={currentLayout === 'masonry'}
			class:lg:columns-3={currentLayout === 'masonry'}
			class:gap-5={currentLayout === 'masonry'}
			class:min-h-[400px]={currentLayout === 'layered'}
			class:md:min-h-[500px]={currentLayout === 'layered'}
			class:lg:min-h-[600px]={currentLayout === 'layered'}
			class:relative={currentLayout === 'layered'}
			class:perspective-1500={currentLayout === 'layered'}
		>
			{#each galleryImages.slice(0, 6) as src, index}
				<div
					class="gallery-item mb-5 inline-block w-full overflow-hidden rounded-xl shadow-[0_10px_30px_rgba(0,0,0,0.3)] transition-all duration-[400ms] ease-[cubic-bezier(0.25,0.8,0.25,1)]"
					class:absolute={currentLayout === 'layered'}
					class:w-[60%]={currentLayout === 'layered'}
					class:md:w-[45%]={currentLayout === 'layered'}
					class:lg:w-[35%]={currentLayout === 'layered'}
					class:max-w-[450px]={currentLayout === 'layered'}
					style={currentLayout === 'layered'
						? `
						left: ${Math.random() * 60 - 30 + 30}%;
						top: ${Math.random() * 60 - 30 + 30}%;
						transform: rotate(${Math.random() * 16 - 8}deg) translateZ(${Math.random() * 200 - 100}px);
					`
						: ''}
				>
					<img
						{src}
						alt="Screenshot showcasing AuraFlow's {src.split('text=')[1]?.replace(/\+/g, ' ') ||
							'software feature'} capability."
						class="block h-auto w-full"
						loading="lazy"
					/>
				</div>
			{/each}
		</div>

		<!-- Print-friendly gallery grid -->
		<div class="print-visible mx-auto grid max-w-5xl grid-cols-3 gap-6 print:block">
			{#each galleryImages.slice(0, 6) as src, index}
				<div class="overflow-hidden rounded-xl border border-gray-200 bg-white shadow-lg">
					<div
						class="flex h-32 items-center justify-center bg-gradient-to-br from-purple-400 to-blue-500 font-bold text-white"
					>
						{src.split('text=')[1]?.replace(/\+/g, ' ') || 'Feature'}
					</div>
					<div class="p-4">
						<h4 class="mb-1 text-sm font-semibold text-gray-800">
							{src.split('text=')[1]?.replace(/\+/g, ' ') || 'Feature'}
						</h4>
						<p class="text-xs text-gray-600">Professional design tools for modern teams</p>
					</div>
				</div>
			{/each}
		</div>
	</div>
</section>

<!-- Page 5: Social Proof & CTA Section -->
<section
	id="social-proof-cta"
	class="page-section min-h-screen bg-[#161617] py-16 text-[#f5f5f7] print:min-h-[8.27in] print:bg-white print:text-[#161617]"
>
	<div class="reveal container mx-auto max-w-6xl px-4">
		<!-- Social Proof -->
		<div class="mb-16 text-center">
			<h3
				class="text-[clamp(1.5rem,4vw,2.5rem)] font-semibold tracking-tight text-[#0071e3] print:text-[2rem]"
			>
				Loved by the world's most innovative teams
			</h3>
			<div
				class="print-hidden logo-cloud perspective-800 mt-12 flex flex-wrap items-center justify-center gap-12 md:gap-16"
			>
				{#each ['apple.com', 'google.com', 'spotify.com', 'dropbox.com', 'notion.so'] as domain, index}
					<img
						src="https://logo.clearbit.com/{domain}"
						alt="{domain.split('.')[0]} logo"
						class="max-h-10 animate-pulse opacity-70 brightness-[2.5] grayscale filter transition-all duration-300 hover:scale-110 hover:opacity-100 hover:brightness-100 hover:filter-none"
						style="animation-delay: -{index * 2}s;"
					/>
				{/each}
			</div>

			<!-- Print-friendly logos -->
			<div class="print-visible mx-auto mt-12 grid max-w-4xl grid-cols-5 gap-8 print:block">
				{#each ['Apple', 'Google', 'Spotify', 'Dropbox', 'Notion'] as company}
					<div class="text-center">
						<div
							class="mx-auto mb-2 flex h-16 w-16 items-center justify-center rounded-full bg-gray-200 text-lg font-bold text-gray-600"
						>
							{company.charAt(0)}
						</div>
						<p class="text-sm font-medium text-gray-700">{company}</p>
					</div>
				{/each}
			</div>
		</div>

		<!-- CTA Section -->
		<div class="text-center">
			<div class="mb-12 flex items-center justify-center">
				<!-- Interactive iPad for screen -->
				<div
					class="print-hidden ipad-pro-container relative transition-transform duration-[400ms] hover:-translate-y-5"
				>
					<div
						class="ipad-pro mx-auto w-[300px] max-w-[400px] rounded-[28px] bg-[#2e2e30] p-4 shadow-[0_40px_80px_-20px_rgba(0,0,0,0.5)]"
						style="height: calc(300px * 1.414);"
					>
						<div
							class="screen h-full w-full rounded-[18px] bg-black bg-cover bg-center"
							style="background-image: url('https://placehold.co/1200x1696/0071e3/FFFFFF?text=Start+Your+Free+Trial');"
						></div>
					</div>
					<div
						class="apple-pencil absolute top-10 right-[-20px] z-10 h-4 w-[90%] origin-top-right translate-x-[-50%] translate-y-[50%] rotate-90 transform rounded-[10px] bg-[#e6e6e6] shadow-[0_5px_15px_rgba(0,0,0,0.2)]"
					></div>
				</div>

				<!-- Print-friendly CTA visual -->
				<div
					class="print-visible h-96 w-80 items-center justify-center rounded-3xl bg-gradient-to-br from-[#0071e3] to-[#0056b3] text-white shadow-2xl print:flex"
				>
					<div class="p-8 text-center">
						<div class="mb-6 text-6xl">🚀</div>
						<h3 class="mb-4 text-2xl font-bold">Ready to Start?</h3>
						<p class="text-lg opacity-90">Transform your workflow today</p>
					</div>
				</div>
			</div>

			<h2
				class="text-[clamp(2.5rem,6vw,4rem)] leading-tight font-semibold tracking-tight text-[#0071e3] print:text-[3rem]"
			>
				The perfect tool for<br />modern creative teams
			</h2>
			<p
				class="mx-auto mt-5 max-w-2xl text-[clamp(1rem,2.5vw,1.25rem)] leading-relaxed text-[#a1a1a6] print:text-lg print:text-[#666]"
			>
				Start your free 14-day trial today. No credit card required.
			</p>

			<div class="mt-12 space-y-4">
				<a
					href="#/"
					class="print-hidden inline-block rounded-full bg-[#0071e3] px-8 py-4 text-lg font-medium text-white no-underline transition-all duration-200 hover:scale-105 hover:bg-[#0077ed]"
				>
					Get Started Free
				</a>

				<!-- Print-friendly CTA -->
				<div
					class="print-visible mx-auto max-w-md rounded-2xl border-2 border-[#0071e3] p-8 print:block"
				>
					<h4 class="mb-4 text-xl font-bold text-[#0071e3]">Start Your Free Trial</h4>
					<div class="space-y-2 text-sm text-gray-700">
						<div class="flex items-center">
							<span class="mr-2">✓</span> 14-day free trial
						</div>
						<div class="flex items-center">
							<span class="mr-2">✓</span> No credit card required
						</div>
						<div class="flex items-center">
							<span class="mr-2">✓</span> Full feature access
						</div>
						<div class="flex items-center">
							<span class="mr-2">✓</span> Cancel anytime
						</div>
					</div>
					<div class="mt-6 text-center">
						<p class="text-lg font-bold text-[#0071e3]">Visit: auraflow.com</p>
						<p class="mt-2 text-sm text-gray-600">Get started in under 2 minutes</p>
					</div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	:global(html) {
		scroll-behavior: smooth;
		font-family:
			-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
		-webkit-font-smoothing: antialiased;
		-moz-osx-font-smoothing: grayscale;
	}

	:global(body) {
		background-color: #161617;
		color: #f5f5f7;
		overflow-x: hidden;
	}

	:global(.reveal) {
		opacity: 0;
		transform: translateY(50px);
		transition:
			opacity 0.8s ease-out,
			transform 0.8s ease-out;
	}

	:global(.reveal.animate-in) {
		opacity: 1;
		transform: translateY(0);
	}

	.page-section {
		display: flex;
		flex-direction: column;
		justify-content: center;
	}

	.perspective-800 {
		perspective: 800px;
	}

	.perspective-1500 {
		perspective: 1500px;
	}

	.perspective-2000 {
		perspective: 2000px;
	}

	/* Print optimizations */
	@media print {
		:global(*) {
			-webkit-print-color-adjust: exact !important;
			color-adjust: exact !important;
			print-color-adjust: exact !important;
		}

		.page-section {
			display: flex !important;
			min-height: 8.27in !important;
			page-break-inside: avoid !important;
			page-break-after: always !important;
			padding: 0.5in !important;
			box-sizing: border-box !important;
		}

		.page-section:last-child {
			page-break-after: avoid !important;
		}

		:global(.print-hidden) {
			display: none !important;
		}

		:global(.print-visible) {
			display: block !important;
		}

		/* Ensure backgrounds print */
		:global(.bg-\\[\\#0071e3\\]) {
			background-color: #0071e3 !important;
		}

		:global(.bg-gradient-to-br) {
			background: linear-gradient(to bottom right, #0071e3, #0056b3) !important;
		}

		/* Text colors for print */
		:global(.text-white) {
			color: white !important;
		}

		:global(.text-\\[\\#0071e3\\]) {
			color: #0071e3 !important;
		}

		/* Grid layouts for print */
		:global(.grid) {
			display: grid !important;
		}

		:global(.grid-cols-2) {
			grid-template-columns: repeat(2, minmax(0, 1fr)) !important;
		}

		:global(.grid-cols-3) {
			grid-template-columns: repeat(3, minmax(0, 1fr)) !important;
		}

		:global(.grid-cols-5) {
			grid-template-columns: repeat(5, minmax(0, 1fr)) !important;
		}
	}

	/* Screen-specific responsive adjustments */
	@media screen and (max-width: 768px) {
		.page-section {
			min-height: 100vh;
			padding: 2rem 0;
		}
	}
</style>
